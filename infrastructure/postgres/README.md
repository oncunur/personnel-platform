# PostgreSQL

PostgreSQL is provisioned by `docker-compose.yml` for local development. EF Core owns application schema migrations. No business table should be created through ad-hoc SQL in production.
